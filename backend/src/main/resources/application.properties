spring.application.name=backend

# MYSQL configuration
spring.datasource.url = jdbc:mysql://localhost:3306/chatify_db
spring.datasource.username=root
spring.datasource.password = root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

# Port number
server.port=8080

# MongoDB configuration
spring.data.mongodb.uri=mongodb://localhost:27017/chatify_messages

# ─── JWT ──────────────────────────────────────────────
#jwt.secret=${JWT_SECRET:thisIsAVeryLongSecretKeyForDevelopmentOnly2024}
jwt.expiration=86400000

# ─── Logging ──────────────────────────────────────────
logging.level.root=INFO
logging.level.com.chatify=DEBUG
logging.level.org.springframework.security=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ─── File Upload (for avatars later) ──────────────────
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

jwt.secret=${JWT_SECRET:3f8d2a1b9c7e4f6d0a5b8c3e2f1d4a7b9c6e3f0d2a5b8c1e4f7d0a3b6c9e2f5}




#chatify-backend/
#??? src/main/java/com/chatify/
#?   ??? config/          ? Security config, WebSocket config
#?   ??? controller/      ? REST API endpoints (what the frontend calls)
#?   ??? service/         ? Business logic (the brain of the app)
#?   ??? repository/      ? Database queries (talks to MySQL and MongoDB)
#?   ??? model/           ? Data models (User, Message, ChatRoom)
#?   ??? dto/             ? Data Transfer Objects (what you send/receive via API)
#?   ??? security/        ? JWT filter, token generation
#?   ??? ChatifyApplication.java  ? Main entry point
#??? src/main/resources/
#?   ??? application.properties   ? All configuration
#??? src/test/            ? All your tests live here